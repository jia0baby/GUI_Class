
git clone https://github.com/jia0baby/GUI_Class.git
          拉檔案

git add .
          向Lacal git 新增所有檔案

git config - -global user.email "313iris00@gmail.com"
          
git config - -global user.name "jia0baby"
          設定使用者帳戶資訊

git commit -m "First Commit"
          標記該次Push的註解
								
git push -u origin main
          將程式碼Push到GitHub上